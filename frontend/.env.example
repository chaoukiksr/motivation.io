# Copy this file to .env and fill in the values.
# The Vite dev server proxies /api → VITE_API_BASE_URL automatically (see vite.config.js).
# Only needed if you override the proxy target.
#VITE_API_BASE_URL=http://localhost:8000

# For production, set this in your deployment environment.
# Example:
 VITE_API_BASE_URL=https://site--motivation--ghs2hdv9b7rc.code.run
